"""A minimal stand-in package, written for this public extract.

The private `job_cv_agent` package is much larger and is not published. This one
holds only the two Gmail helpers the digest imports: `email_delivery.send_job_email`
and the token paths and client builder in `gmail_inbox`.
"""
